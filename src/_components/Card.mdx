export function Card({ title, id, children }) {
    return (
        <section id={id} class="c-card">
            <h2 class="c-card__title">{title}</h2>
            <div class="c-card__content">
                {children}
            </div>
        </section>
    )
}