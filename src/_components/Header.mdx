export function Header() {
    const nav = [
        { label: "Pedagógiai Hitvallásunk", url: "#hitvallasunk" },
        { label: "Baba-mama", url: "#baba-mama" },
        { label: "Szülőknek", url: "#szuloknek" },
        { label: "Felvételi", url: "#felveteli" },
        { label: "Dokumentumok", url: "#dokumentumok" },
        { label: "Elérhetőségeink", url: "#elerhetoseg" },
    ];
    
    return (
        <>
            <header class="c-header">
                <div class="c-header__logo">
                    <span class="c-header__logo-1">Török Sándor</span>
                    <span class="c-header__logo-2">Waldorf<br/>Óvoda</span>
                </div>
            </header>
            <nav class="c-nav" aria-label="Főmenü">
                <span class="c-nav__indicator" aria-hidden="true" />
                {nav.map((item) => (
                    <a href={item.url} key={item.url} class="c-nav__link">{item.label}</a>
                ))}
            </nav>
        </>
    )
}