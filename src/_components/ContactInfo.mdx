{/* ContactInfo – definition-list style contact details. Styles: ContactInfo.css */}

export function ContactInfo({ title, id, children }) {
  return (
    <div id={id} class="c-contact">
      <h2 class="c-contact__title">{title}</h2>
      <div class="c-contact__content">
        {children}
      </div>
    </div>
  )
}
