export function Intro({ title, children }) {
    return (
        <section class="c-intro">
            <h1>{title}</h1>
            <div class="c-intro__content">
                {children}
            </div>
        </section>
    )
}